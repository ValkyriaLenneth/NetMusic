- `createGlobalStyle` create the global CSS style 
- `taggedTemplateLiteral` the style of  styled.div\`css\`
- `styled(component)`` \` Create styled component  
- `styled.div``\` 

- `&selector` use selector
- `${style['theme-color']}` use exported global-style
